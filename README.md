# claude-dev
Dockerfile with necessary tooling to run claude-code

## Using this

### Prerequisites
- Docker installed

### Running

1. Build the docker image.
  From the root directory of the project run `docker build -t claude-dev:latest .`

